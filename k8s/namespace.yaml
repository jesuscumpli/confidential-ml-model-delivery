apiVersion: v1
kind: Namespace
metadata:
  name: confidential-ml
  labels:
    # Pod Security Standards: every workload in this namespace must be "restricted".
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
